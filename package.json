{
  "name": "hydrocomplete-dag",
  "version": "0.1.0",
  "private": true,
  "description": "WASM model builder for HydroComplete — a visual hydrology/hydraulics DAG editor for stormwater analysis.",
  "license": "MIT",
  "homepage": "https://hydrocomplete.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/mf4633/hydrocomplete-dag.git"
  },
  "keywords": ["wasm", "hydrology", "hydraulics", "stormwater", "civil-engineering", "dag-editor"],
  "type": "commonjs",
  "scripts": {
    "build": "wasm-pack build --target web --out-dir www/pkg --release",
    "serve": "node serve.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
